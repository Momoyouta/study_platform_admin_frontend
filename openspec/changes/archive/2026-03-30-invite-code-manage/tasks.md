## 1. 表单字段与交互改造

- [x] 1.1 在 `src/pages/SchoolManage/InviteCodeManage/index.jsx` 的创建弹窗中新增 `course_id` 表单项。
- [x] 1.2 根据 `type === 2`（学生加入课程）实现 `course_id` 字段的动态展示与隐藏。
- [x] 1.3 为 `course_id` 增加仅在课程邀请码场景生效的必填校验，并在类型切换时清理无效值。

## 2. 创建请求与类型契约同步

- [x] 2.1 调整创建 payload 组装逻辑：课程邀请码提交时携带 `course_id`，其他类型不以该字段为必填。
- [x] 2.2 检查并更新邀请码相关类型定义（如 `src/type/invite.ts`），补充 `course_id` 字段说明。
- [x] 2.3 确认 `createInvite` 调用参数与后端契约一致，避免字段命名或类型偏差。

## 3. 验证与回归

- [x] 3.1 手工验证三类邀请码创建流程，确认仅课程邀请码显示并强制 `course_id`。
- [x] 3.2 验证课程邀请码缺失 `course_id` 时的表单拦截与错误提示。
- [x] 3.3 验证非课程邀请码提交成功且不受新增字段规则影响。

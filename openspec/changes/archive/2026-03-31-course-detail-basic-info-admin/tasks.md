## 1. 路由与导航改造

- [x] 1.1 在路由配置中新增 `/courseDetail` 页面路由并保持现有管理端布局壳不变
- [x] 1.2 修改课程列表页“编辑”操作为跳转到 `/courseDetail?courseId=<id>`
- [x] 1.3 在详情页初始化时校验 `courseId`，缺失时展示错误提示并阻止详情请求

## 2. 课程列表字段调整

- [x] 2.1 调整课程列表列定义，新增学校名称列（`school_name`）
- [x] 2.2 移除章节数（`chapter_count`）和课时数（`total_lesson_count`）列
- [x] 2.3 回归验证平台管理员与学校管理员视角下的列表查询与分页行为

## 3. 课程详情基础信息页面

- [x] 3.1 新建课程详情页面与样式文件，按设计实现基础信息页签与内容区域
- [x] 3.2 接入 `GET /course/getCourseBasicAdmin/{id}` 并展示返回的全部基础字段（可编辑与只读分区）
- [x] 3.3 使用 Ant Design 表单实现可编辑字段：`name`、`cover_img`、`status`
- [x] 3.4 在详情页接入 `TempImageUpload`，将上传返回路径写入 `cover_img` 并支持预览回显

## 4. 更新提交与交互反馈

- [x] 4.1 提交时仅组装 `id`、`name`、`cover_img`、`status` 到 `PUT /course/updateCourseAdmin` 请求体
- [x] 4.2 处理保存成功/失败提示，并在成功后刷新详情数据
- [x] 4.3 确认可编辑字段外的数据均为只读展示且不会进入更新 payload

## 5. 联调与验收

- [x] 5.1 完成从列表进入详情、修改基础信息、保存并回显的端到端手工验收
- [x] 5.2 覆盖异常路径：无 courseId、详情查询失败、更新失败、图片上传失败
- [x] 5.3 执行项目构建与 lint（如已配置）并修复本次变更引入的问题
